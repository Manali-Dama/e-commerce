model orders{
    order_id Int @id @default(autoincrement())
    customer_id Int 
    products String[]
    bill_amount Float
    order_status status
}

enum status{
    order_recieved
    conformed
    dispatched
    delivered
    cancelled
}